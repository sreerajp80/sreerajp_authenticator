{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path','') or d.get('tool_response',{}).get('filePath',''); exit(0 if f.endswith('.dart') else 1)\" && cd 'l:/Android/SreerajP_Authenticator/sreerajp_authenticator' && flutter test 2>&1 || true",
            "timeout": 120,
            "statusMessage": "Running flutter test..."
          }
        ]
      }
    ]
  }
}
