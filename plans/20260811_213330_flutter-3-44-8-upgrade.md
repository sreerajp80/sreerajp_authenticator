# Upgrade Flutter SDK to 3.44.8 and Dart SDK to 3.12.2

**Status:** Proposed

## Issue
The local Flutter environment has been updated to Flutter 3.44.8 and Dart 3.12.2. The project's `pubspec.yaml` environment SDK constraint and project metadata (`AGENTS.md`, `CLAUDE.md`) still target `^3.11.1`.

## Files to Change
- `pubspec.yaml`
- `AGENTS.md`
- `CLAUDE.md`

## Proposed Fix
1. Update `pubspec.yaml` environment SDK constraint from `^3.11.1` to `^3.12.2`.
2. Update Flutter SDK (`^3.44.8`) and Dart SDK (`^3.12.2`) specifications in `AGENTS.md` and `CLAUDE.md`.
3. Run `flutter pub get`, `flutter analyze`, and `flutter test` to verify dependency resolution, static analysis, and unit test health on Flutter 3.44.8 / Dart 3.12.2.
